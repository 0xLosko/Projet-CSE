<form method="post" action="{{ path('app_admin_delete', {'id': admin.id}) }}" onsubmit="return confirm('êtes vous sûr de vouloir supprimer ce compte utilisateur ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ admin.id) }}">
    <button class="btn">Supprimer</button>
</form>
