<div class="footer-con">
    <div class="footer-img-con">
        <img src="{{ asset('images/logo.png') }}">
    </div>
    <div class="footer-link-con">
        <h3>CSE Lycée Saint-Vincent</h3>
        <div class="footer-link-wrapper">
            <div>
                <a href="{{ path('partnership') }}">&rsaquo; Partenariats</a>
                <a href="{{ path('about') }}">&rsaquo; A propos de nous</a>
            </div>
            <div>
                <a href="{{ path("ticketing") }}">&rsaquo; Billetterie</a>
                <a href="{{ path("contact") }}">&rsaquo; Contact</a>
            </div>
        </div>
    </div>
    <div class="footer-newsletter-con">
        <h4>Inscrivez-vous aux offres</h4>
        <p>Reçevez un email à chaque nouvelle offre<br>sur le site internet.</p>
        <div>
            <form action="{{ path('subscribe') }}" method="POST">
                <input type="text" placeholder="Saisissez-votre email" name="email">
                <button>S'inscrire</button>
            </form>
        </div>
    </div>

    {% for message in app.flashes('success') %}
        <div class="form-row">
            {{ message }}
        </div>
    {% endfor %}
</div>
